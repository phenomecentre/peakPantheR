## Import Tab ------------------------------------------------------------------

#importUI
# spectraPaths, CSVParams, spectraMetadata, cpdMetadata
# load on trigger with initialise_annotation_from_files_UI_helper()

# check both annotations are valid
