language: r
cache: packages

os:
  - linux

r:
  - bioc-devel
  - bioc-release

matrix:
  allow_failures:
    - r: bioc-devel 

addons:
  apt:
    packages:
      - libnetcdf-dev
      - netcdf-bin
      - libhdf5-dev
